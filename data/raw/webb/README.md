# Webb Raw Inputs

Local raw files currently present:
- `webb_ai.pdf`

Current use:
- the paper PDF is mirrored locally for provenance
- normalized occupation-level Webb benchmark variables are currently read from the mirrored `GPTs-are-GPTs` `autoScores.csv` extract because that is the cleanest public machine-readable source found for the current benchmark pipeline

Important note:
- this source is benchmark-only and is not used in public `2.0` runtime scoring
