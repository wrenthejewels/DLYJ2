Archived route aliases that are no longer part of the active site structure.

These folders previously existed as alternate entry points:
- `model3/`
- `guide3/`
- `method3/`

The live site now uses:
- `/` for the model
- `/guide` for the guide
- `/method` for the methodology
